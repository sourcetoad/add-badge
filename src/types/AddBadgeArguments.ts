export default interface AddBadgeArguments {
  backgroundColor: string;
  config?: string;
  dryRun?: boolean;
  fontFile?: string;
  fontSize: number;
  gravity: string;
  input: string;
  mode?: string;
  output?: string;
  position?: string;
  shadowColor: string;
  text: string;
  textColor: string;
}
