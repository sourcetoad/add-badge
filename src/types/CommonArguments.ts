export default interface CommonArguments {
  backgroundColor: string;
  badgeText: string;
  dryRun?: boolean;
  fontFile?: string;
  fontSize: number;
  gravity: string;
  position?: string;
  shadowColor: string;
  textColor: string;
}
