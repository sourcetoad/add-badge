export default interface CommonArguments {
  backgroundColor: string;
  badgeText: string;
  dryRun?: boolean;
  fontFile?: string;
  fontSize: number;
  gravity: string;
  paddingX: number;
  paddingY: number;
  shadowColor: string;
  textColor: string;
}
